node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Backups do Firestore gerados por scripts/exportar-banco.ts.
# Contêm dados de pacientes (nome, contato, fichas de anamnese) — nunca entram no repositório.
backup/
package-lock.json
